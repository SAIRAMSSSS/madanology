services:
  - type: web
    name: madanology-vedic
    env: node
    plan: free
    buildCommand: "npm run postinstall && npm run build"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: ASTROLOGY_API_PROVIDER
        value: mock
